Whenever there is an execution with time T then we must have a task T.
Whenever there is an execution with machine M then we must have a machine M.

// DEFINED
Whenever we have a sequence with first starting time T1, with first machine M1,
                            with second starting time T2, with second machine M2, with time T,
    then the difference between the starting with time T1, with machine M1 and the starting with time T2, with machine M2 must be less than or equal to -T.

// EXTERNAL
It is prohibited that the difference between start with ... and 0 is not less than B, whenever ...